# 2026-08-10 dev 栈主要流程手测
- 测试类型: 核心路径（API 旅程；Claude 无浏览器，UI 交互闭环未覆盖）
- 测试环境: 192.168.100.248 dev 栈（docker-compose.yml + .env.dev，1ms.run mirror）
- 测试结果: **有条件通过**（API 旅程 ✅，UI 闭环未覆盖）
- 发现问题: 1（P2）；遗留已知: 1（publish 未实现）
- 测试时间: ~30 分钟

## 与上次测试差异
- 新增覆盖: dev 栈部署后首次手测
- 修复验证: **form 创建 500（change4 fieldSchema 默认）→ 测试环境 201 ✅**
- 新增闭环: 留资端到端（建表单→提交→线索脱敏）✅

## 测试结果（§12.2）

### 冒烟 §0.5（端点可达）
全部端点 401/404/200 = 可达 ✅（401=需 JWT）。public `/pages?path=` 404（site 不存在正确）、`/config` 200（公开，无需鉴权）。

### API 旅程（JWT，handtest/admin）
| 旅程 | 步骤 | 结果 |
|------|------|------|
| 1 认证 | POST /api/auth/login | ✅ JWT（handtest/admin）|
| 2 发布 | 建站→建页 | ✅（publish 端点未实现，T24）|
| 3 留资 | 建表单→提交→线索 | ✅ 端到端（phone 138****1180 脱敏）|

### UI E2E（Playwright 全量，测试环境，§13 自动化）
e2e 全量（57）= **16 passed / 38 failed / 3 skipped**。含 auth.setup **UI 登录闭环 ✅**（浏览器打开 engine 5174 → 填 handtest → 进 dashboard → token）。
- 16 passed 验证：测试环境部署稳定，UI 登录 + 基础 API flow 可用
- 38 failed 三类：① 未实现端点 ~25（AB/AI/billing/collab/datasource/feature-gate/quota/publish，story 待做）② lead submit 400 ~7（缺陷 1 formId body）③ ssr-deep ~3（新发现：不存在路径/slug 返回 200 应 404；by-path 端点 404）

**新发现缺陷（ssr）**:
| ID | 级别 | 描述 | 状态 |
|----|------|------|------|
| 2 | P2 | website SSR 对不存在 slug/路径返回 200 应返回 404（SEO/正确性） | 新建 |

## 缺陷清单
| ID | 级别 | 模块 | 描述 | 状态 |
|----|------|------|------|------|
| 1 | P2 | 留资 submit | `LeadSubmitRequest.formId` 是 body `@NotBlank`，但 BFF path `/forms/{formId}/submit` 不注入 body；前端/e2e 需传 body.formId（e2e lead-variants 400 同因） | 新建 |

## 未覆盖
| 功能 | 原因 | 建议 |
|------|------|------|
| 发布闭环 publish | 端点未实现（T24）| 待 story |
| UI 交互闭环 | Claude 无浏览器 | Playwright/人工 |
| 设计器操作（拖拽/属性/撤销）| 同上 | engine-e2e |

## 手测→自动化建议（§13）
- P0: 登录 + 留资闭环 → 已有 e2e/lead-variants（修 formId body 后应转绿）
- P1: 发布闭环 → 待 publish 端点（T24）实现后补 e2e/publish-flow
- P2: form 创建 → 补单测（fieldSchema 默认）
