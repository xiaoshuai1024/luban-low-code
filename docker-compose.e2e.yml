# Luban 跨项目 E2E 服务编排
#
# 一键起齐：MySQL + Redis + Java backend + BFF + engine + website
# 用法：make e2e-up / make e2e-down
#
# 端口约定（与 e2e/.env.example 对齐）：
#   MySQL     3307（避免与本地 3306 冲突）
#   Redis     6380（避免与本地 6379 冲突）
#   Java      8080  context-path /backend
#   BFF       3100（宿主）→ 容器内 3000（engine nginx 写死反代 bff:3000）
#   engine    4200  (nginx listen 4200)
#   website   3000（宿主）→ 容器内 4173（nitro 默认端口）
#
# Go 后端已废弃（Q4=C，2026-06-28），E2E 不再起 Go 后端。

services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: luban
      MYSQL_DATABASE: luban
      MYSQL_CHARSET: utf8mb4
      MYSQL_COLLATION: utf8mb4_unicode_ci
    ports:
      - "3307:3306"
    volumes:
      - e2e-mysql:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-pluban"]
      interval: 5s
      timeout: 5s
      retries: 20

  redis:
    image: redis:7-alpine
    ports:
      - "6380:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  backend-java:
    build:
      context: ./apps/backend-java
    environment:
      APP_PORT: "8080"
      MYSQL_HOST: mysql
      MYSQL_PORT: "3306"
      MYSQL_DB: luban
      MYSQL_USER: root
      MYSQL_PASSWORD: luban
      REDIS_HOST: redis
      REDIS_PORT: "6379"
    ports:
      - "8080:8080"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy

  bff:
    build:
      context: ./apps/bff
    environment:
      # 默认指向 Java 后端（/backend context-path）
      BACKEND_BASE_URL: "http://backend-java:8080/backend"
      # 容器内监听 3000（engine nginx 写死反代 bff:3000）；宿主经 3100 访问
      PORT: "3000"
    ports:
      - "3100:3000"
    depends_on:
      - backend-java

  engine:
    build:
      context: .
      dockerfile: apps/engine/Dockerfile
    environment:
      VITE_BFF_BASE_URL: "http://127.0.0.1:3100"
    depends_on:
      - bff
    ports:
      - "4200:4200"

  website:
    build:
      context: .
      dockerfile: apps/website/Dockerfile
    environment:
      # SSR 走容器间 DNS 调 BFF（容器内 3000）；公开页渲染用
      NUXT_PUBLIC_BFF_BASE_URL: "http://bff:3000"
      NUXT_PUBLIC_DEFAULT_SITE_SLUG: "default"
    ports:
      - "3000:4173"
    depends_on:
      - bff

volumes:
  e2e-mysql:
